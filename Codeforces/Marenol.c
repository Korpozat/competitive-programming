#include<stdio.h>
#include<string.h>

int main(){
    int n;
    scanf("%d", &n);
    for (int i=1; i<=n; i++){
        int length;
        scanf("%d", &length);
        char s1[length+1];
        char s2[length+1];

        //base cases:
        if (length <= 2 && s1 != s2){
            printf("False")
        }

        counter1 = 0
        counter2 = 0

        
    }
}